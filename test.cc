// #include "quadratic_probing.h"
// #include "linear_probing.h"
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <cstring>
#include <vector>
using namespace std;

string ToLower(string word) //converts the words to lowercase
{
  string converted;
  for(int i = 0; i < word.length(); i++)
  {
    converted += tolower(word[i]);
  }
  return converted;
}
int main()
{


    
}